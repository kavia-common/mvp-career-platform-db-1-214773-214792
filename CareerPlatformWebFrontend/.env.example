# Example local development environment file (optional)
# This file is NOT used during Docker builds and is ignored by .dockerignore
# Copy to .env and adjust values as needed for local development.

REACT_APP_API_BASE_URL=/api/v1
REACT_APP_ENV=development
